{
  "name": "@agentic-swarm/orchestrator",
  "version": "0.0.1",
  "private": true,
  "description": "Orchestrator that coordinates specialist agents for build requests",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agentic-swarm/shared": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@agentic-swarm/config": "workspace:*",
    "@types/node": "^25.2.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^4.0.18"
  }
}
